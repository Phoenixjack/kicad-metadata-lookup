# KiCad Metadata Lookup

Experimental GUI tool for looking up missing KiCad component metadata through user-configured provider APIs.

KML is intentionally separate from KiCad Import Assistant while the lookup workflow is explored. Local API keys belong in `kml_private_data.json`, which is ignored by Git. Use `kml_private_data.example.json` as the tracked template.
